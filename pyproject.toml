[project]
name = "email-reading-agent"
version = "0.1.0"
description = "AI agent that reads emails, classifies importance, and shows important ones on a dashboard."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.6",
    "uvicorn[standard]>=0.34.0",
    "openai>=1.59.6",
    "python-dotenv>=1.0.1",
    "jinja2>=3.1.5",
]

[tool.uv]
package = false
